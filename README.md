
  # High-Fidelity UI Prototype (Copy)

  This is a code bundle for High-Fidelity UI Prototype (Copy). The original project is available at https://www.figma.com/design/KL5QPchmZWYtNMcGcSr6aY/High-Fidelity-UI-Prototype--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  